<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.teleconta.pas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirstApptutorial"
        tools:targetApi="31">


        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FirstApptutorial">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.HomeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FirstApptutorial">
        </activity>

        <activity android:name=".activities.OpenBillingActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FirstApptutorial">
        </activity>

        <activity android:name=".activities.PaidBillingsActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FirstApptutorial">
        </activity>

        <activity android:name=".activities.UserDataActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FirstApptutorial">
        </activity>

        <activity android:name=".activities.ChooseLineActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FirstApptutorial">
        </activity>

        <activity android:name=".activities.ViewServicesActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FirstApptutorial">
        </activity>

        <activity android:name=".activities.SolicitationMainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FirstApptutorial">
        </activity>

        <activity android:name=".activities.CreateSolicitationChooseLineActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FirstApptutorial">
        </activity>

        <activity android:name=".activities.CreateSolicitationChooseTypeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FirstApptutorial">
        </activity>

        <activity android:name=".activities.CreateSolicitationMain"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FirstApptutorial">
        </activity>
    </application>

</manifest>